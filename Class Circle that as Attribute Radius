class Circle:
    def __init__(self, radius):
        self.radius = radius

    def area(self):
        pi = 3.1416
        return pi * (self.radius ** 2)

c1 = Circle(5)
print("Area of The Circle:", c1.area())
